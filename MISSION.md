# Mission: 백엔드 개발자로 자립하기

## Why

프론트엔드 개발자로 일하다 백엔드 개발자로 직종을 전환했다. 현재 Performance Plus(Rails 7, Ruby, Grape API, MySQL) 프로젝트에서 실무 작업을 하고 있지만, MVC 구조, DB 개념, 인증/인가 등 백엔드 기초 없이 작업하고 있다. 작업을 통해 개념을 연결하고, 혼자서도 기능을 설계하고 구현할 수 있는 수준이 되는 것이 목표다.

2026년 9월부터 자체 프로젝트 **뿌(부동산을 부탁해)** 의 백엔드를 전담하게 되면서, 학습 소스가 두 갈래가 됐다.

## 두 갈래 학습 소스

| | Performance Plus (실무) | 뿌 (자체 프로젝트) |
|---|---|---|
| 스택 | Rails 7.2 · Grape · MySQL · Sidekiq · Pundit · Karafka | Java 21 · Spring Boot · PostgreSQL + PostGIS |
| 내 롤 | 백엔드 (요구사항을 받아 구현·리뷰) | 백엔드 전담 + 기획 협업 |
| 배우는 힘 | **남이 설계한 시스템을 읽는 힘** | **아무것도 없는 데서 설계하는 힘** |
| 문제의 성격 | 동시성 · 트랜잭션 · 락 | 데이터 파이프라인 · 멱등성 · 외부 API 정합성 |

두 갈래를 같이 가는 이유는 **프레임워크 특수 지식과 백엔드 보편 지식을 갈라내기 위해서**다.
트랜잭션 경계·격리수준·N+1·인덱스는 양쪽에 다 있지만, `let_it_be` 오염이나 Grape `Boolean`
강제변환은 Rails 전용이다. 한쪽만 파면 이 둘이 구분되지 않는다.

## Success looks like

- 새 기능 요구사항을 받았을 때, 스키마 → 모델 → 요청 경로 → 응답 계약 순서를 스스로 설계할 수 있다
- 코드를 보고 "이 요청이 어떤 경로로 흘러가는지" 머릿속에 그릴 수 있다
- N+1 쿼리를 스스로 발견하고 수정할 수 있다
- 테스트를 보고 무엇을 검증하는지 읽을 수 있고, 새 테스트를 스스로 작성할 수 있다
- 백그라운드 잡을 이해하고 직접 작성할 수 있다
- **빈 저장소에서 모듈 구조·스키마·배포 경로를 스스로 설계할 수 있다** (뿌)
- **같은 개념이 Rails 와 Spring 에서 어떻게 다르게 구현되는지 설명할 수 있다**

## Constraints

- 강의를 듣는 것이 아니라 **작업을 통해** 학습한다 — 실무에서 마주친 코드, 또는 뿌에서 실제로 구현한 것
- 순서대로 배울 수 없다 — 프로젝트 요구에 따라 순서가 바뀐다
- 프론트엔드(React, TypeScript) 지식은 충분하다 — 비유로 적극 활용할 것
- **Java 는 처음이다** — Ruby/JS 비유가 안 통하는 지점(정적 타입, DI, 영속성 컨텍스트, 프록시 기반 AOP)은 A-Z 로 간다
- 뿌는 실무가 아니라 자체 프로젝트다 — 일정 압박이 없는 대신 **내가 안 짜면 아무도 안 짠다**

## Out of scope

- 프론트엔드 개발 (이미 알고 있음)
- Spring 외의 다른 백엔드 프레임워크 (Django, Go 등)
- 뿌의 배포에 필요한 범위를 넘는 DevOps
